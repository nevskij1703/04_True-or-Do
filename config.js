/**
 * config.js — глобальные настройки игры.
 * Меняй значения тут, чтобы регулировать частоту рекламы,
 * интенсивность по умолчанию, звук/вибрацию и т.д.
 */
window.GAME_CONFIG = {
  // === Реклама ===
  // Сколько карточек показать без рекламы в начале игры.
  cardsBeforeFirstAd: 5,
  // Минимум карточек между двумя interstitial-рекламами.
  minCardsBetweenAds: 4,
  // Максимум карточек между двумя interstitial-рекламами (случайно в диапазоне).
  maxCardsBetweenAds: 6,
  // Если true — реклама будет мок-объектом (для разработки и тестов).
  // На проде поставь false и подключи Яндекс-рекламу в ads.js.
  mockAds: true,

  // === Геймплей ===
  // Максимальная интенсивность по умолчанию (1..5). Можно менять в настройках.
  defaultMaxIntensity: 3,
  // Сколько ходов длится временно открытая премиум-категория после rewarded-рекламы.
  rewardedUnlockDuration: 10,
  // Через сколько карточек запускать конфетти.
  confettiEvery: 20,

  // === Звук и вибрация ===
  enableVibration: true,
  enableSound: false, // звук по умолчанию выключен, чтобы не пугать

  // === Контент ===
  // Названия режимов и их фильтр по категориям.
  // category — это категория карточки (см. cards.js).
  modes: {
    soft:      { label: 'Нежный',     categories: ['нежные', 'для разговора'] },
    flirt:     { label: 'Флирт',      categories: ['флирт', 'нежные'] },
    romance:   { label: 'Романтика',  categories: ['романтические', 'нежные', 'для разговора'] },
    funny:     { label: 'Смешной',    categories: ['смешные', 'лёгкие челленджи'] },
    mixed:     { label: 'Смешанный',  categories: ['нежные', 'флирт', 'романтические', 'смешные', 'для разговора', 'лёгкие челленджи'] }
  },

  // === Dev ===
  // Список запрещённых слов для проверки контента в dev-панели.
  blacklist: [
    'алкоголь', 'водка', 'пиво', 'виски', 'наркотик',
    'удар', 'насил', 'кровь', 'смерть',
    'политик', 'религи', 'измен'
  ]
};
